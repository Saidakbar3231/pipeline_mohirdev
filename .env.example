# Copy to .env and fill in real values. Never commit .env.

# Gemini / Google AI Studio key (Yo'l 2 pipeline)
GEMINI_API_KEY=

# MohirDev STT API key (Yo'l 1 pipeline) — users can also paste in UI
STT_API_KEY=

# App access password for the login screen
ACCESS_PASSWORD=aisha2026

# Flask session secret — generate a random string for production
APP_SECRET_KEY=change-me-to-a-random-string
